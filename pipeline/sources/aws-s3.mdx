---
title: "AWS S3"
description: "配置 AWS S3 对象存储数据源"
---

## AWS S3

AWS S3（Amazon Simple Storage Service）是亚马逊云提供的对象存储服务。

### 如何获取鉴权参数

1. 登录 [AWS 控制台](https://console.aws.amazon.com/)
2. 进入 IAM 服务，创建用户或使用现有用户
3. 为用户创建访问密钥（Access Key）：
   - 在用户详情页，选择"安全凭证"标签
   - 点击"创建访问密钥"
   - 保存 `Access Key ID` 和 `Secret Access Key`
4. 确保用户具有对应存储桶的读取权限，至少包括：
   - `s3:ListBucket`：列出存储桶中的对象
   - `s3:GetObject`：获取对象内容

### 端点 URL 格式

- 标准端点：`https://s3.{region}.amazonaws.com`
- 例如：`https://s3.us-east-1.amazonaws.com`

### 配置示例

```python
'source': {
    'type': 's3',
    'endpoint': 'https://s3.us-east-1.amazonaws.com',
    'access_key': 'your-access-key-id',
    'secret_key': 'your-secret-access-key',
    'bucket': 'your-bucket-name',
    'prefix': 'documents/',  # 可选
    'region': 'us-east-1'
}
```

### 使用示例

```python
from xparse_pipeline import create_pipeline_from_config

config = {
    'source': {
        'type': 's3',
        'endpoint': 'https://s3.us-east-1.amazonaws.com',
        'access_key': 'your-access-key-id',
        'secret_key': 'your-secret-access-key',
        'bucket': 'my-documents',
        'prefix': 'pdfs/',
        'region': 'us-east-1'
    },
    # ... 其他配置
}

pipeline = create_pipeline_from_config(config)
pipeline.run()
```

