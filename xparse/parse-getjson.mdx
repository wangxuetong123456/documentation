---
title: "获取JSON"
---

当您想要获取JSON结果并以.json格式文件保存时，您可以参考以下示例代码。

这部分代码在[快速启动](/xparse/parse-quickstart)中已有示例，您可以直接使用。

```python
# 保存完整的JSON响应到result.json文件
with open("result.json", "w", encoding="utf-8") as f:
    f.write(response)
```